# 🚀 Быстрый старт (Getting Started)

Руководство по настройке проекта Kanban Board с AI

---

## 📋 Содержание

1. [Требования](#требования)
2. [Клонирование проекта](#клонирование-проекта)
3. [Получение API ключей](#получение-api-ключей)
   - [Groq API (обязательно)](#groq-api-обязательно)
   - [Telegram Bot (для Phase 2)](#telegram-bot-для-phase-2)
   - [Google Gemini (опционально)](#google-gemini-опционально)
4. [Настройка окружения](#настройка-окружения)
5. [Запуск проекта](#запуск-проекта)
6. [Проверка работы](#проверка-работы)

---

## Требования

- **Python 3.11+** - [Скачать](https://www.python.org/downloads/)
- **Node.js 18+** - [Скачать](https://nodejs.org/)
- **Git** - [Скачать](https://git-scm.com/downloads)

---

## Клонирование проекта

```bash
# Клонировать репозиторий
git clone <url-репозитория>

# Перейти в директорию
cd kanban-board

# Установить зависимости backend
cd backend
pip install -r requirements.txt

# Установить зависимости frontend
cd ../frontend
npm install
```

---

## Получение API ключей

### Groq API (обязательно)

Groq - это **бесплатный** AI API с щедрыми лимитами. Рекомендуется как основной провайдер.

**Шаг 1:** Перейдите на сайт
```
https://console.groq.com
```

**Шаг 2:** Регистрация
- Нажмите "Sign Up"
- Можно использовать email или GitHub аккаунт
- Подтвердите email

**Шаг 3:** Получение ключа
1. Войдите в консоль
2. В меню слева выберите "API Keys"
3. Нажмите кнопку "Create API Key"
4. Придумайте название (например, "kanban-board-dev")
5. Нажмите "Submit"
6. **Скопируйте ключ** (начинается с `gsk_`)

```
⚠️ Важно: Ключ показывается только один раз!
   Сохраните его в безопасном месте.
```

**Бесплатные лимиты:**
- 1,000,000 токенов в минуту
- 1,000,000,000 токенов в день
- Доступные модели: Llama 3, Mixtral, Gemma

---

### Telegram Bot (для Phase 2)

Необходим для интеграции с Telegram.

**Шаг 1:** Откройте Telegram
- Найдите бота **@BotFather** (официальный бот для создания ботов)

**Шаг 2:** Создайте нового бота
- Отправьте команду: `/newbot`
- Введите имя бота (отображается пользователям): `My Kanban Board`
- Введите username (должен заканчиваться на "bot"): `my_kanban_bot`

**Шаг 3:** Получите токен
- BotFather отправит сообщение с токеном:
```
Done! Congratulations on your new bot. You will find it at t.me/my_kanban_bot
Use this token to access the HTTP API:
123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
```

- **Скопируйте токен** (формат: `цифры:буквы-и-цифры`)

**Шаг 4:** Настройте бота (опционально)
```
/setdescription - описание бота
/setabouttext - информация о боте
/setuserpic - аватарка
/setcommands - команды меню
```

**Команды для бота:**
```
tasks - Показать все задачи
add - Добавить новую задачу  
done - Отметить задачу выполненной
help - Справка
```

---

### Google Gemini (опционально)

Резервный AI API на случай проблем с Groq.

**Шаг 1:** Перейдите на сайт
```
https://ai.google.dev
```

**Шаг 2:** Получение ключа
1. Нажмите "Get API key in Google AI Studio"
2. Войдите в Google аккаунт
3. Нажмите "Create API key"
4. Выберите проект (или создайте новый)
5. Нажмите "Create API key in existing project"
6. **Скопируйте ключ**

**Бесплатные лимиты:**
- 60 запросов в минуту
- 1,000,000 токенов входных данных в минуту

---

### Google Calendar (для Phase 2)

Для синхронизации сроков задач с календарем.

**Шаг 1:** Google Cloud Console
```
https://console.cloud.google.com
```

**Шаг 2:** Создание проекта
1. Нажмите на выпадающий список проектов (вверху)
2. Нажмите "New Project"
3. Введите название: "Kanban Board"
4. Нажмите "Create"

**Шаг 3:** Включение API
1. В меню слева выберите "APIs & Services" → "Library"
2. Найдите "Google Calendar API"
3. Нажмите "Enable"

**Шаг 4:** Создание credentials
1. Перейдите в "APIs & Services" → "Credentials"
2. Нажмите "Create Credentials" → "OAuth client ID"
3. Настройте экран согласия (Consent Screen):
   - User Type: External
   - App name: "Kanban Board"
   - User support email: ваш email
   - Developer contact: ваш email
   - Сохраните

4. Создайте OAuth client ID:
   - Application type: Web application
   - Name: "Kanban Board Web"
   - Authorized redirect URIs:
     ```
     http://localhost:8000/auth/callback
     ```
   - Нажмите "Create"

5. **Скопируйте Client ID и Client Secret**

---

## Настройка окружения

**Шаг 1:** Создайте файл `.env`

В корне проекта выполните:
```bash
# Windows:
copy .env.example .env

# Linux/Mac:
cp .env.example .env
```

**Шаг 2:** Отредактируйте `.env`

Откройте файл `.env` в текстовом редакторе и замените placeholder-ы на реальные ключи:

```bash
# ===========================================
# Обязательно (для Phase 1)
# ===========================================

# Groq API - Primary AI provider
GROQ_API_KEY=gsk_YxBza5gHLo5hZ2lKj7VWcDEfGhIjKlMnOpQrStUvWxYz

# ===========================================
# Для Phase 2 (интеграции)
# ===========================================

# Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Google Calendar (опционально)
GOOGLE_CLIENT_ID=123456789012-abcdefghijklmnopqrstuvwxyz123456.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-abcdefghijklmnopqrstuvwxyz

# ===========================================
# Опционально (резервный AI)
# ===========================================

# Google Gemini - Fallback
GEMINI_API_KEY=AIzaSyBcdefghijklmnopqrstuvwxyz123456789
```

```
💡 Совет: Для начала достаточно только GROQ_API_KEY
   Остальные можно добавить позже при необходимости.
```

---

## Запуск проекта

### Backend

```bash
# Перейти в директорию backend
cd backend

# Создать директорию для данных
mkdir -p ../data/tasks/todo ../data/tasks/in_progress ../data/tasks/done

# Запустить сервер
python -m app.main

# Или с uvicorn:
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер запустится на http://localhost:8000

**Документация API:** http://localhost:8000/docs

### Frontend

```bash
# В новом терминале, перейти в директорию frontend
cd frontend

# Запустить dev-сервер
npm run dev
```

Приложение откроется на http://localhost:5173

---

## Проверка работы

### 1. Проверка Backend

Откройте в браузере:
```
http://localhost:8000/health
```

Должно вернуть:
```json
{"status": "healthy"}
```

### 2. Проверка API

Откройте Swagger UI:
```
http://localhost:8000/docs
```

Должна открыться документация API с возможностью тестирования.

### 3. Проверка Frontend

Откройте:
```
http://localhost:5173
```

Должна загрузиться страница Kanban Board (или заглушка, если UI еще не готов).

### 4. Проверка AI

В Swagger UI протестируйте endpoint:
```
POST /api/ai/analyze
```

Или через curl:
```bash
curl -X POST http://localhost:8000/api/ai/analyze \
  -H "Content-Type: application/json" \
  -d '{"task_id": "test-001"}'
```

---

## 🆘 Устранение неполадок

### Ошибка: "Module not found"

```bash
# Убедитесь что установлены зависимости
pip install -r requirements.txt
npm install
```

### Ошибка: "GROQ_API_KEY not found"

```bash
# Проверьте что файл .env создан
ls -la  # должен быть файл .env

# Проверьте что ключ вписан
cat .env | grep GROQ
```

### Ошибка: "Port already in use"

```bash
# Backend - сменить порт
uvicorn app.main:app --port 8001

# Frontend - сменить порт
npm run dev -- --port 5174
```

### Ошибка: "CORS error"

```bash
# Убедитесь что в .env указан правильный FRONTEND_URL
FRONTEND_URL=http://localhost:5173
```

---

## 📚 Следующие шаги

После успешного запуска:

1. **Phase 1** - Разработка MVP (Kanban доска)
2. **Phase 2** - Интеграции (Telegram, Calendar)
3. **Phase 3** - AI функции

См. файл `kanban-board-plan.md` для детального плана.

---

## 💡 Полезные ссылки

- **Groq Console:** https://console.groq.com
- **Groq Docs:** https://console.groq.com/docs
- **Telegram Bot API:** https://core.telegram.org/bots/api
- **Google AI Studio:** https://ai.google.dev
- **FastAPI Docs:** https://fastapi.tiangolo.com
- **React Docs:** https://react.dev

---

**🎉 Готово! Теперь можно начинать разработку!**

Если возникли проблемы - проверьте:
1. ✅ Все зависимости установлены
2. ✅ Файл `.env` создан и заполнен
3. ✅ Все сервисы запущены (backend + frontend)
4. ✅ API ключи валидны (проверьте в консолях)
